# SPEC — 스터디 허브 (2026-07-23 표면 재구축 개정)

> 상태: owner 승인(2026-07-23 재지시 — 표면 전면 재구축·하부 유지). 구판(2026-07-22 P1~P3 체계)을 덮어씀(이력 = git).
> 개정 사유: P1~P3 산출 표면에 대한 오너 판정 = 재시작. 멤버 페이지 제거·메인 과거 디자인 복귀·기사 재설계·세미나/실습 보류.
> 루브릭 = 거버넌스 헌장 §0(R1~R5). 구현 모델 = opus.

## §1 의의 [비규범]

사이트 = 광운대 경영학부 MIS×AI 스터디의 허브. 세미나·실습의 산출물이 쌓이는 무대이며, 기사 기고제로 "스터디원 = 생산 주체"를 유지한다. 멤버 개인 페이지 축은 2026-07-23 오너 결정으로 내렸다 — 멤버의 생산 표면은 기고(저자 표기)로 수렴.

## §2 구조 결정 (R1 — 확정 레코드 7)

| # | 항목 | 확정값 |
|---|------|--------|
| 1 | 기존 사이트 관계 | 재건. URL 유지(https://kwu-erpclub.github.io/) |
| 2 | 기사 콘텐츠 생산 | 멤버 기고제(`content/기사/` 직접 push) |
| 3 | 반영 통제 | 영역별 차등(§6) |
| 4 | 기술 스택 | Vite + React MPA(정적, 백엔드 없음) |
| 5 | 디자인 | 메인 = 과거 v3 소개형 복귀(구 `App.jsx`+`home.css`, 2026-07-23 오너). 기사 = 토스 테크·우아한형제들 기술블로그 리스트 문법 참조. 색·타이포·상태 토큰 = `../docs/디자인규칙-메인.md` v2 준수 |
| 6 | 멤버 페이지 | **전부 제거**(목록+개인 auto-glob 엔트리, 2026-07-23 오너). 재도입 = 오너 재승인 필요 |
| 7 | 세미나·실습 페이지 | **보류** — 목적 재정의 대기(일정 안내가 아니라 세미나 전 자료 업로드 + 사후 재현 가이드). 페이지·콘텐츠 계약은 현상 유지, 재설계 = 별도 spec |

## §3 저장소·배포 (R4)

- 소스 = KWU-ERPCLUB/kwu-erpclub.github.io 공개 repo. 멤버 쓰기 = collaborator 등록.
- 배포 = GitHub Pages(main 반영 → 자동 배포). CI = GitHub Actions.
- `빌드 실패 ∨ §5 스키마 검증 실패 → merge·배포 차단`
- 웹앱 규격 = 워크스페이스 §8 적용(app/CLAUDE.md · vitest · oxlint · roadmap.md).

## §4 페이지 레코드 (R1)

| 페이지 | 경로 | 내용 | 데이터 원천 | 쓰기 주체 | 상태 |
|--------|------|------|------------|----------|------|
| 메인 | `/` | 과거 v3 소개형(WHY·ROADMAP·PROJECTS·FAQ, 섹션=페이지 강조 로직) | 코드 내 상수 + 문안-메인.md | 오너(공용) | 재구축 대상 |
| 기사 | `/articles/` | 기고 목록(태그 칩 필터 + 세로 스택: 날짜·저자·제목·요약) + 상세(본문 문서형) | `content/기사/` | 멤버 전원 | 재구축 대상 |
| 세미나 | `/seminars/` | 회차별 목록 + 상세 | `content/세미나/` | 회차 발제자 | 보류(현상 유지) |
| 실습 | `/labs/` | 실습 가이드 목록 + 상세 | `content/실습/` | 회차 발제자 | 보류(현상 유지) |
| 기존 5종 | `/about/` `/join/` `/log/` `/reports/` `/projects/` | 구판 유지 | 각 페이지 | 오너 | 범위 밖(유지) |

- 모집 마케팅 카피 금지 승계: `grep {지원하기, 모집중, 신청} = 0`.

## §5 콘텐츠 데이터 계약 (R1 + R4 — 하부, 유지)

**시스템 고정 3 (멤버 수정 불가 — §6 공용 영역)**:
1. 위치·파일명 = `content/<종류>/YYYY-MM-DD-<작성자id>-<슬러그>.md` (종류 enum = {기사, 세미나, 실습})
2. frontmatter 스키마 — 공통 = `{title, author, date}` / 기사 += `{source_url, source_name, tags ⊂ {에이전트, 채용시장, 거버넌스, 도구, 기타}}` / 세미나 += `{회차(number), 유형 ∈ {인지, 실습}}` / 실습 += `{연계회차(number), tools[]}`
3. 목록·상세 렌더 코드 = 시스템 소유. 스키마 유일 원천 = `app/src/content/schema.js`

**멤버 재량 3 (규범 판정 없음)**: 생산 방식(수동/프롬프트 루틴/자동 루프, 실행 환경 = 멤버 본인 것) · 본문 마크다운 구성 · 기고 빈도·소재.

**검증 규칙**: `필수 필드 결측 ∨ tags·유형이 enum 밖 ∨ 파일명 패턴 불일치 → CI 실패`

## §6 반영 통제 (R4)

- `변경 경로 ⊆ content/ → 직접 push 허용`
- `변경 경로에 그 외(공용 코드·설정·data/) 포함 → PR + owner 승인` (CODEOWNERS로 강제)
- `CI 실패 → merge·배포 차단`

## §7 완료기준 — 표면 재구축 (R3 — 전부 T/F)

- CR1: 메인 렌더 = 구 v3 소개형(현황판 요소 grep {trackCard, 최근 기고} = 0) ∧ hub.css 미로드
- CR2: `/members/` 관련 산출물 = 빌드 dist에 0건 ∧ 내비에 MEMBERS 링크 0건 ∧ vite.config에 멤버 글롭 코드 0건
- CR3: 기사 목록 = 태그 칩 필터 존재 ∧ 세로 스택 리스트(날짜·저자·제목·요약 4요소) ∧ 상세 본문 max-width ≤ 720px
- CR4: `npm run validate` ∧ `npm test` ∧ `npm run build` 전부 통과
- CR5: 3뷰포트(375/768/1440) 렌더 확인 ∧ 오너 육안 승인 1회
- CR6: 승계 게이트 유지 — 스키마 위반 파일 → CI 실패(기존 실증 유지) ∧ 기존 URL 응답 200

## §8 비범위 (out of scope)

- 세미나·실습 페이지 재설계(목적 재정의 후 별도 spec)
- 멤버 페이지 재도입(오너 재승인 전 금지)
- 시스템 측 자동 수집·크롤링 파이프라인 / 로그인·백엔드
- 세미나 커리큘럼 자체의 확정(세미나 트랙 설계 문서 별도)

## 메타 게이트 자가체크

- MG1 금지어휘(규범 문장) = 0건 (§1은 `[비규범]`) → PASS
- MG2 상태·분류값 enum(콘텐츠 종류·tags·유형·페이지 상태) → PASS
- MG3 완료기준 CR1~CR6 T/F 판정가능 → PASS
- MG4 조건부 규칙(§3·§5·§6) if-then → PASS
