# SPEC — 스터디 허브 (2026-08-20 5차 개정: 실측 정합 — 페이지 세트·프로젝트 상세·frontmatter)

> **IA 4차(2026-08-05 오너 인터뷰)**: ① 브랜드 = **AIM 단독 표기 + 산하 한 줄**(나브·히어로·푸터·title/meta·RSS — 계보는 ROADMAP ORIGIN·FAQ로 유지) ② **about 폐지** — 운영 증빙 3종은 /recruit 하단 이관, WhyNow 통계·계보 타임라인은 소멸(메인 ROADMAP 중복) ③ **log 폐지** — 데이터(`app/src/data/log.js`)는 워크스페이스 공지 탭 '운영 기록'으로 읽기 전용 내부화. 나브 노출 페이지 7종→**4종 + 로그인 워크스페이스**. 재도입 = 오너 재승인. 아래 §2·§4의 about·log 서술 = 이력(4차로 대체). 빌드 엔트리 실측 = §2-2(5차).

> **5차(2026-08-20 정합 개정 — 구조 변경 아님, 코드 실측 반영)**: ①§2·CR1 페이지 세트 = 빌드 엔트리 실측으로 교체(전용 프로젝트 상세 2종 추가) ②§4 projects = md 상세·프롬프트 로그 → **전용 인터랙티브 상세**(md = 목록 카드 메타 전용) ③§4 세미나 표지 = "썸네일 2장 겹침" → 표지 1장 ④§5 frontmatter = 스키마 실측으로 교체(기사 설명·태그, 세미나 pdf·특강·공개·일정미정) ⑤워크스페이스 이후 개편은 별도 spec에 위임(§9).
>
> 상태: owner 확정(2026-07-27 — AIM 스터디 통합 IA 7종·모집 카피 규칙 개정. AIM 정의 = `../docs/specs/2026-07-27-학습정의.md`). 2차(2026-07-23 사용자 정의·IA 개편)를 덮어씀(이력 = git).
> 2026-08-05 정합 수정(구조 변경 아님 — 코드 현행 반영): §5 기사 주제 enum 값 나열 제거(원천 = `app/src/content/schema.js` 참조로 치환).
> 2026-08-07 정합 수정: 인사이트 목록의 시각 골격 서술을 이 문서에서 제거 — **단일원천 = `../docs/디자인규칙-메인.md` §6-1**(2026-08-06 4차 개편 이후 이 문서의 "2열 색면 카드" 서술이 낡아 단일원천 위반이던 것 해소).
> 개정 근거: 오너 인터뷰(주/부 사용자·심장박동·페이지 통폐합) + deep-research 검증(리서치 원문은 2026-08-12 삭제 — git 이력 `../docs/_archive/2026-07-23-허브-페이지요소-리서치.md`).
> 정체성·카피 상위 기준 = `../docs/정체성-북극성.md`(판정 P1~P4). 디자인 = `../docs/디자인규칙-메인.md` v2 + 2026-08-06 4차 개편(§3-1 페이지 패밀리 규격 포함).
> 루브릭 = 거버넌스 헌장 §0(R1~R5).

## §1 의의 [비규범]

사이트 = 광운대 경영학부 AIM(AI×MIS) 스터디의 허브. **주 사용자 = 스터디원**(반복 방문하는 작업공간·자료창) / **부 사용자 = 멤버가 외부에 성과를 보여줄 때의 상대**(스터디 운영 방식 + 개별 기여 증빙 — 유입 트래픽은 목표 아님). 심장박동 = **정보 공유**: 기사(이슈 인지·아카이빙) → 세미나(선별 실습)의 파이프라인이 스터디 엔진이고 사이트는 그 무대다. 멤버 개인의 생산 표면 = 기고 저자 표기(비공개 개인공간은 백엔드 사안 — §8 별도 트랙).

## §2 구조 결정 (R1 — 확정 레코드)

| # | 항목 | 확정값 |
|---|------|--------|
| 1 | 기존 사이트 관계 | 재건. URL 유지(https://kwu-erpclub.github.io/) |
| 2 | 페이지 세트 | **빌드 엔트리 8종(5차 실측 — 원천 = `app/vite.config.js` rollupOptions.input)**: 메인·인사이트·세미나·모집·projects·**projects/adsp**·**projects/site**(전용 인터랙티브 상세)·워크스페이스(로그인). + 정적 `/guide/` 1종(빌드 엔트리 아님). 나브 노출 = 4종 + 로그인 시 워크스페이스. **about·log = 폐지(4차)**, labs·reports·join = 제거 유지 |
| 3 | 메인 성격 | **하이브리드** — 상단 = v3 소개형(정체성·간판) + 하단 = 최근 활동(살아있는 도구면). 1차 CR1(현황 요소 0) 폐지 |
| 4 | 기사 정체 | "진짜 기사" 아님 — **이슈 아카이브**. 표면명 = **AI 인사이트 / INSIGHTS / `/insights/`**(2026-07-23 오너 — "이슈 스캔 아카이브"·ARTICLES 기각: 실무 문법 아님·AI/MIS 키워드 부재). 계약 내부 종류명 = `기사` 유지(§5 — 폴더·스키마 안정성) |
| 5 | 세미나 정체 | 이슈 중 **직접 해볼 것 선별 실습**. 상세 = 3블록(준비·진행·재현) — Carpentries 표준 이식. **공급원 = AIM 세션(2026-07-27 통합)**: AIM 커리큘럼의 세션(개념·실습)이 회차로 축적 — 별도 세미나 트랙 없음 |
| 5b | AIM 관계 | 사이트 = AIM 스터디(헌장 §2 `id=aim`)의 활동 무대이자 **주체 표기**(4차 — 브랜드 AIM 단독). 세미나=세션 아카이브 / 모집 탭=기수 안내+운영 증빙 / 운영 기록=워크스페이스 공지 탭 / projects=산출물 증빙 |
| 6 | 분류 체계 | **글 성격 × 업무 영역 + '지금 써먹기' 플래그**(2026-07-23 딥리서치 2차 — 구 용도/기술 2축 폐기: 한 축에 이질 기준 혼재 진단). 근거 문서는 2026-08-12 삭제(git 이력 `../docs/_archive/2026-07-23-분류축-리서치.md`) |
| 7 | 기술 스택 | Vite + React MPA(정적, 백엔드 없음) 유지 |
| 8 | 멤버 페이지·개인공간 | 공개 멤버 페이지 = 제거 유지(1차 결정). **비공개 개인공간(백엔드) = 별도 spec**(§8) |
| 9 | 상호작용 장치 | 검색 = 클라이언트 부분일치(제목·요약) / RSS = 전체 피드 1개 / giscus 댓글 = 보류(오너 재판단) |
| 10 | 디자인 | 1차 승계(메인 v3·기사 토스/우아한 문법) + **§3-1 페이지 패밀리 규격 전 페이지 적용** |

## §3 저장소·배포 (R4 — 유지)

- 소스 = KWU-ERPCLUB/kwu-erpclub.github.io 공개 repo. 멤버 쓰기 = collaborator.
- 배포 = GitHub Pages. **브랜치 2종(2026-08-19 베타 전환)**: `main` = 배포판(push → 자동 배포) / `dev` = 작업대(push → 검증만, 배포 없음). 코드 변경은 `dev`에 모아 릴리스 시 머지하고, `main` 직행은 콘텐츠·오타정정·장애수리 3종만(spec §5).
- 버전 = `app/package.json` 단일원천. 릴리스마다 태그 `vX.Y.Z` + `CHANGELOG.md` 갱신. 절차·게이트 = `erp-club/docs/specs/2026-08-19-릴리스-체계.md`.
- CI: `빌드 실패 ∨ §5 검증 실패 → merge·배포 차단`.
- 웹앱 규격 = 워크스페이스 §8(app/CLAUDE.md · vitest · oxlint · roadmap.md).

## §4 페이지 레코드 (R1)

| 페이지 | 경로 | 내용 | 데이터 원천 | 상태 |
|--------|------|------|------------|------|
| 메인 | `/` | 상단 소개(v3: WHY·ROADMAP·FAQ 승계) + **AIM 모집 공고 블록(2026-07-27 신설 — 상단 소개 직하, 모집 탭 링크. `모집 기간 아님 → "다음 기수 안내"로 전환`)** + **하단 최근 활동(최근 기사 ≤3 + 최근/예정 세미나 1)** | 코드 상수 + content 로더 | 개편(모집 블록 추가) |
| 인사이트 | `/insights/` | 기능 요구: 검색(제목·요약 부분일치) + 성격·주제 필터 + 기간(월) 셀렉트(콘텐츠에 쌓인 월만) + `고정`=true 상단 핀 · 썸네일(`이미지` 선택, fallback 규칙 = thumb-resolver) · 0건 = 디자인된 빈 상태 · 720px 상세 · 상세·성격 탭 = URL 반영(`?p=`·`?tab=`, 뒤로가기 복원). **시각 골격(피처·목록·필터 배치) = 디자인규칙 §6-1 단일원천**(2026-08-07 — 이 문서에서 서술 제거). **좌측 탭·허브 4섹션·월별 그룹 목록 = 폐지(2026-07-24 구조 개혁)** — 월은 필터로만 존재. 설명 카피 = 경영·MIS·AI 키워드 필수 | `content/기사/` | 개편 |
| 세미나 | `/seminars/` | **v3 타임라인 아카이브**(2026-07-25 오너 — Linear changelog 변형, NEXT 히어로·예정/과거 이분 폐지): ① 상단 간단 소개 1~2구 ② 필터 바(주제 탭[전체+등장 주제] + 정렬 토글 최신↔과거) ③ **세로 날짜 타임라인**(좌 날짜 레일·점) × 항목 = 제목(→상세) + **표지 1장 카드**(5차 — `썸네일` 배열의 첫 장, 없으면 비표시) + 주제·유형·회차 메타 + 간략 설명(요점 또는 발췌). 최신 = 맨 위 크게. 상세(v3.1 오너) = **슬라이드 버튼 제거**(발표자료 진입 = 목록 썸네일 카드 단일화) + 밴드 아래 [발제 블록(author) → "다루는 내용" 번호 목차형 → "출처" 리스트] 특수 렌더(실습 3블록 로직은 유지). 강조색 상향(v3.1 오너 — §0-2 기록) | `content/세미나/` | 재설계(2026-07-25) |
| **모집** | `/recruit/` | **AIM 기수 안내(2026-07-27 신설)**: AIM 소개(웹 표기용 개조식 — 북극성 비표기 규칙 준수) + 1기 로드맵(페이즈) + 모집 요강(대상·활동·기간 — 사실 서술) + 참여 방법 + **운영 증빙 3종(4차 — 구 about Proof 이관, content 집계·출처 표기)**. 기수 전환 시 이 탭만 갱신 | 페이지 코드 + content 집계 | 신설(4차 증빙 추가) |
| projects | `/projects/` | **DPM 문법**(2026-07-24 오너): 대형 커버(화면 캡처) 그리드 + 제목·한줄 설명 + **호버 오버레이(GitHub·Web)**. **상세 = 전용 인터랙티브 페이지 2종(5차)** — 목록 카드가 전용 URL(`/projects/adsp/`·`/projects/site/`)로 보낸다(구 `?p=` 딥링크도 매핑). **md = 목록 카드 메타 전용**(커버·제목·설명·상태·링크) — 구 "md 상세 + 프롬프트 로그" 렌더 경로는 폐지 | `content/프로젝트/`(카드 메타) + 전용 페이지 코드(상세) | 재설계(5차 2026-08-20) |
| 제거 | `/labs/` `/reports/` `/join/` + **`/about/` `/log/`(4차 2026-08-05)** | 페이지·엔트리·전용 코드 삭제(404 허용 — 유입 낮음, 오너 확정). log 데이터 = 워크스페이스 공지 탭 '운영 기록'으로 내부화 | — | 제거 |

- 모집 카피 규칙(2026-07-27 개정 — AIM 1기 모집이 사실이 되어 전제 변경): **모집 사실 서술 허용**(요강·기간·참여 방법 — 개조식). 마케팅 어투 금지로 교체: `grep {지금 바로, 놓치지, 마지막 기회, 서두르, 얼른} = 0` ∧ 과장·권유투 금지(디자인규칙 §0-1 카피 원칙 승계). 구 금지어 {지원하기, 모집중, 신청} = 사실 문장 내 사용 허용.
- 안티패턴 금지: 범용 "자료실"류 페이지 신설 금지(Yale dumping-ground 근거).

## §5 콘텐츠 데이터 계약 v2 (R1 + R4)

**시스템 고정 (멤버 수정 불가 — §6 공용 영역)**:
1. 위치·파일명 = `content/<종류>/YYYY-MM-DD-<작성자id>-<슬러그>.md` (종류 enum = **{기사, 세미나, 프로젝트}** — 실습 폐지, 프로젝트 2026-07-24 콘텐츠화)
2. frontmatter — 공통 = `{title, author, date}`. **필드 집합·enum 값의 유일 원천 = `app/src/content/schema.js`**(검증기 CLI·앱 로더 공용). 아래는 그 요약이며, 어긋나면 스키마가 맞다 — 이 절을 고친다. 값 집합(성격 4값·주제 6값)은 여기에 나열하지 않는다(재낡음 방지)
   - 기사 += 필수 `{source_url, source_name, 성격(단일·enum), 주제(단일·enum), 설명(1줄 — 카드 표시, 본문 발췌 대체 2026-07-27)}` / 선택 `{태그(문자열 1~5 배열·`#` 없이·표시 전용), 지금써먹기(boolean), 시각(HH:MM), 이미지(문자열 — 썸네일·히어로), 이미지설명(1줄 — `이미지` 동반), 시리즈(레지스트리 id — 원천 `content/series.js`), 고정(boolean — 허브 뷰 상단 핀)}`. **`이미지` = 선택**(시리즈 고정 커버·자동 루틴 발행은 이미지 없음이 정상). 구 tags·용도/기술 폐지. 성격 4값 정의·판정 질문 = `CONTRIBUTING.md`
   - 세미나 += 필수 `{유형 ∈ {인지, 실습}, 회차(숫자 — 단 `특강=true`면 금지)}` / 선택 `{특강(boolean — 정규 차시 밖 단발, 번호 대신 '특강' 라벨), 공개(boolean·미기재=공개 — false면 공개 목록에서 제외), pdf(발표자료 PDF 경로), 슬라이드(웹 덱 URL), 썸네일(배열 1~2 — 첫 장이 표지), 주제(단일 — 기사 주제 enum 재사용), 발원기사, 요점(배열 1~4), 장소, 일정미정(boolean — 날짜 자리 '일정 미정')}`. 목록 = 시간 역순 단일 타임라인(date>오늘 ∨ 일정미정 = "예정" 배지). 상세 렌더 = 웹 에디토리얼 롱폼 유지
   - 프로젝트 += 필수 `{설명(1줄), 상태 ∈ {운영 중, 진행 중, 보관}}` / 선택 `{커버(/img/projects/ 캡처 경로), github(URL), web(URL)}`. **본문 = 목록 카드 아래 짧은 소개까지**(5차 — 상세는 전용 페이지 담당, 구 `## 프롬프트 로그` 섹션 규약 폐지)
3. 세미나 본문 규칙: `유형 = 실습 → 필수 헤딩 {## 준비, ## 진행, ## 재현 가이드} 전부 존재` (CI 검증). 인지 = 자유 구성 — 권장 헤딩 {## 다루는 내용(→ 상세 목차형 특수 렌더), ## 출처(→ 출처 리스트 특수 렌더)} (2026-07-25 v3.1 — CI 비강제)
4. 스키마 유일 원천 = `app/src/content/schema.js`(검증기·앱 로더 공용)

**멤버 재량 (판정 없음)**: 생산 방식 · 본문 구성(§5-3 제외) · 기고 빈도·소재.

**검증**: `필수 필드 결측 ∨ enum 밖 값 ∨ 파일명 패턴 불일치 ∨ §5-3 위반 → CI 실패`

## §6 반영 통제 (R4 — 유지)

- `변경 경로 ⊆ content/ → 직접 push 허용` / `그 외 포함 → PR + owner 승인`(CODEOWNERS) / `CI 실패 → 차단`

## §7 완료기준 (R3 — 전부 T/F)

- CR1: 페이지 세트 = §2-2 빌드 엔트리 8종(vite.config.js 실측 일치) ∧ labs·reports·join·about·log 산출물 = dist 0건 ∧ 내비 링크 = 4종(INSIGHTS·SEMINARS·PROJECTS·RECRUIT, +로그인 시 WORKSPACE)만
- CR2: 메인 = 상단 소개 + AIM 모집 블록(모집 탭 링크 동작) + 하단 최근 활동 섹션(최근 기사 ≤3·세미나 1) 렌더
- CR2b: 모집 = AIM 소개·1기 로드맵·요강·참여 방법 렌더 ∧ 마케팅 어투 grep 0(§4 개정 목록) ∧ 북극성 P1(정체성 문장 dist 0건) 유지
- CR3: 인사이트 = 검색·성격·주제·기간 필터 동작 ∧ 목록 골격 = 디자인규칙 §6-1 현행 준수 ∧ 상세 = 디자인 셸(헤더·출처 카드·자유 본문·이전/다음) ∧ 기존 기사 1건 신스키마 이관·통과 ∧ 기고 템플릿(`_template.md`, 스캔 제외) 존재
- CR4: 세미나 = 목록+상세 3블록 렌더 ∧ `유형=실습 ∧ 헤딩 결측 → CI 실패` 실증(위반 픽스처 1건)
- CR5: RSS = 빌드 산출 `rss.xml`(기사 전체) 유효(W3C validator 통과)
- CR6(4차 개정): 운영 증빙 = /recruit 하단 렌더 ∧ 마케팅 어투 grep 0(§4 개정 목록) ∧ 수치 전 항목 출처 각주
- CR7: 전 페이지 §3-1 패밀리 규격 통과(공통 page-head·시그니처 ≥1·디자인된 빈 상태) ∧ 3뷰포트(375/768/1440)
- CR8: `npm run validate` ∧ `npm test` ∧ `npm run build` 통과 ∧ 오너 육안 승인 1회
- CR9: 북극성 판정 P1~P4 통과(정체성 문장 dist 0건 포함)

## §8 비범위 (out of scope)

- **비공개 멤버 개인공간(백엔드: 로그인·DB)** — 별도 브레인스토밍·spec(오너 확정 순서: 공개 허브 먼저)
- giscus 댓글(보류) · 태그별 RSS · 뉴스레터 · 검색 전문(본문) 인덱싱
- 세미나 커리큘럼 자체의 확정(트랙 설계 문서 별도)

## §9 위임 (이 문서가 담지 않는 것 — 별도 spec 단일원천)

이 문서 = **공개 허브의 구조·콘텐츠 계약**. 아래 영역은 각 spec이 원천이며 여기서 값을 중복하지 않는다.

| 영역 | 단일원천 |
|---|---|
| 워크스페이스(로그인 영역) 백엔드·데이터 계층 | `../docs/specs/2026-08-05-워크스페이스-백엔드.md` |
| 워크스페이스 탭 구조(2026-08-14 재편 이후 현행) | `app/CLAUDE.md`(위 spec §0이 위임을 명시) |
| 공고 구독·개인화(세부 분류·캘린더 합류·자동 등록 파이프라인) | `../docs/specs/2026-08-18-공고-구독-개인화.md` |
| 모바일 UI · PWA | `../docs/specs/2026-08-18-모바일-UI.md` |
| 릴리스 체계(브랜치 2종·버전·게이트) | `../docs/specs/2026-08-19-릴리스-체계.md` |
| 디자인 수치 규격 | `../docs/디자인규칙-메인.md` |
| 기고 운영 규칙(사람용 설명) | `CONTRIBUTING.md` · 발행 전 자검 = `ops/기사-검수-하네스.md` |
| 공고 수집 기준·수집처 | `ops/공고-수집-하네스.md` |

## 메타 게이트 자가체크

- MG1 금지어휘(규범 문장) = 0건 (§1은 `[비규범]`) → PASS
- MG2 enum(종류·용도·기술·유형·페이지 상태) → PASS
- MG3 CR1~CR9 T/F 판정가능 → PASS
- MG4 조건부 규칙(§3·§5·§6) if-then → PASS
