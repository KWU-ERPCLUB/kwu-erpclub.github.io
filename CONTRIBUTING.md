# 기고 가이드 — AI 인사이트

광운대학교 ERP연구회 MIS×AI 스터디 허브. 인사이트(`/insights/`) 기고 규칙·절차.
frontmatter 규칙의 유일 원천 = `app/src/content/schema.js`. 이 문서는 그 사람용 설명.

## 기고 흐름 (2026-07-25 개정 — main 직접 push는 오너 전용, 멤버 = PR)

1. **collaborator 등록** — 오너에게 GitHub 계정 전달, 저장소 쓰기 권한 획득
2. **clone** — `git clone https://github.com/KWU-ERPCLUB/kwu-erpclub.github.io.git`
3. **브랜치 생성** — `git switch -c 기고/<작성자id>-<슬러그>`
4. **템플릿 복사** — `app/content/기사/_template.md` → 같은 폴더에 새 파일로 저장
5. **파일명 규칙** — `YYYY-MM-DD-<작성자id>-<슬러그>.md` (예: `2026-08-01-hong-agent-workflow.md`)
   - `<작성자id>` = frontmatter `author` 값과 동일
   - `_` 시작 파일 = 게재·검증 제외(초안·템플릿 보관용)
6. **frontmatter 채움** — 아래 필드 표 참고, 주석 줄 삭제
7. **push + PR 생성** — `git push -u origin <브랜치>` 후 GitHub에서 main으로 Pull Request
8. **자동 게이트** — CI(validate+test+build)가 PR에서 검사. `app/content/` 안만 고친 PR = 체크 통과 시 **본인이 바로 머지 가능**(승인 불필요). 그 밖의 파일(페이지·디자인·설정)을 건드리면 = 오너 승인 없이는 머지 불가(CODEOWNERS+룰셋이 차단)
9. **자동 게재** — main 병합 시 GitHub Pages 자동 배포

> 권한 구조: 사이트 공용 영역(페이지·디자인·설정) 변경 = 오너 전용. 멤버 기고 영역 = `app/content/` 하위만. main 직접 push는 관리자 외 차단(룰셋).

## 소재 선정 기준 (2026-07-30 신설)

기사로 쓸지 말지의 관문. 게재 = 유일한 보관(별도 스크랩 원장 없음 — 관문을 통과한 소재만 흔적이 남는다).

### 관문 3문 — 전부 "예"면 채택

| # | 관문 | 판정 질문 |
|---|------|-----------|
| G1 | 독자 | 경영·MIS 비개발자 스터디원에게 "그래서 우리는?"이 한 줄이라도 나오는가 |
| G2 | 출처 | 주장마다 링크 가능한 출처가 있는가 (아래 출처 등급) |
| G3 | 성격 | 성격 4값 중 하나에 무게중심이 잡히는가 |

- G3 실패 = 소재 미성숙 → 보류 (무게중심이 생기면 다시 판정)

### 출처 등급 — 공식 단독주의 아님

공식 발표만 기다리면 정보가 늦고 실사용 체감과 다르다. 등급을 나눠 화법을 달리해 싣는다:

| 등급 | 소스 | 실을 수 있는 내용 | 화법 |
|------|------|------------------|------|
| A 확정 | 공식 발표·문서·논문·주요 매체 | 출시·가격·스펙 등 사실 | 단정 가능 |
| B 실사용 | 유튜브·GitHub·개발자 블로그·커뮤니티 | 사용감·평가·비교·팁 | 비단정 — "~라는 평가"·"~로 알려짐" |
| C 배제 | 어그리게이터 단독·루머·출처 불명 | 게재 불가 | — |

- B등급 = 적극 활용 대상. 실사용 관점이 공식 발표보다 앞서는 경우가 많다. 단 출처 명기 + 단정 금지가 조건
- 해외(영문) 소스 적극 참조 — 한국 자료만으로 제한하지 않는다. 영문 출처 그대로 링크
- 링크 없는 항목 게재 금지 (사이트 시그니처 — 출처 있는 수치)

### 우대 / 배제

- 우대: ERP×AI 직결 소재 · 따라 할 절차가 있는 소재(`지금써먹기` 후보) · 스터디원 실요청 소재
- 배제: 코드·개발 자체가 주어인 소재(업무·경영 맥락 앵글일 때만 안) · 미출시 루머 단독 · 기존 기사와 중복 · 취업·스펙 앵글 전면
- 스터디원 개인 요청 = 기사로 만들어 게재한다(요청자만이 아니라 전원이 보게). 요청 맥락은 본문의 용도 맥락으로 반영
- **요청 소재의 G1은 요청자 기준으로 판정한다** — 착수 전에 요청자의 용도·눈높이(입문/실무/현업)를 확인하고, 독자 프레임을 임의로 "스터디원 평균"으로 낮추지 않는다(2026-07-30 사후 검수 교훈 — 현업 관점 요청에 학생 과제 프레임으로 응답한 사례)

## AI로 기고하기 (권장)

ChatGPT·Claude 등에 아래를 붙여 넣고 소재(URL)를 주면 규격에 맞는 초안이 나온다. 결과물은 반드시 본인이 읽고 사실 확인 후 제출:

```
너는 광운대 ERP연구회 'AI 인사이트' 기고 도우미다. 내가 주는 원문(URL 또는 내용)을 바탕으로
아래 규격의 마크다운 기고문 1개를 작성하라.

[frontmatter — 전부 필수 규칙 준수]
title(개조식 한 줄·과장 금지) / author(내 아이디) / date(YYYY-MM-DD) / source_url / source_name
/ 성격(트렌드·심층 분석·활용법·튜토리얼·도구·프롬프트 중 1) / 주제(에이전트·모델·플랫폼·워크플로·자동화·거버넌스·리스크·시장·생태계·크리에이티브·미디어 중 1)
/ 설명(카드에 보일 핵심 한 줄) / 태그(선택 — # 없는 문자열 1~5개 배열, 예: [클로드, 에이전트])

[본문 규칙]
- 평서문 단문. 과장·감탄·권유형 수사 금지. 모든 수치에 출처 링크.
- 문단은 짧게(3문장 이하) 끊고 사이에 빈 줄 — 가독성 우선.
- 글 맨 위 ::: 요약 (2~3줄) ::: 블록.
- ## 소제목 3~5개(번호 자동 부여됨). 핵심 문구는 *별표*(강조색), 핵심 숫자는 **볼드**.
- 전문용어 = 위첨자 ¹²³ 표시 후 해당 섹션 끝에 ::: 용어 \n 용어 | 설명 \n ::: 블록(섹션마다 번호 1부터).
- 글 맨 아래 ::: 출처 \n 이름 | URL | 비고 \n ::: 블록에 인용 출처 전부.
```

## frontmatter 필드

| 필드 | 필수 | 값 |
|------|------|-----|
| `title` | 필수 | 글 제목 한 줄 (개조식·AI투 금지) |
| `author` | 필수 | 작성자id (파일명 프리픽스와 동일) |
| `date` | 필수 | `YYYY-MM-DD` |
| `source_url` | 필수 | 원문 URL |
| `source_name` | 필수 | 출처 이름 (예: Stanford HAI) |
| `성격` | 필수 | 단일값 — 아래 성격 4값 중 1개 |
| `주제` | 필수 | 단일값 — 아래 주제 6값 중 1개 |
| `설명` | 필수 | 카드에 보일 핵심 설명 한 줄 — 목록 카드는 본문 대신 이것만 표시 |
| `태그` | 선택 | 문자열 1~5개 배열, `#` 없이 (예: `[클로드, 에이전트]`) — 카드에 #태그로 표시. 표시 전용·필터 없음 |
| `지금써먹기` | 선택 | boolean (기본 false). true = 지금 바로 적용 가능한 실전형 |
| `시각` | 선택 | `HH:MM` — 게재 시각. 카드·상세에서 날짜 옆 표기 (예: `16:51`) |
| `이미지` | 선택 | 문자열 — 목록·허브 썸네일 (경로 `/img/기사/x.png` 또는 https URL) |
| `고정` | 선택 | boolean (기본 false). true = 허브 뷰 해당 성격 섹션 최상단 고정 |

## 성격 4값 (단일 선택)

| 성격 | 정의 | 판정 질문 |
|------|------|-----------|
| 트렌드 | 새 소식·출시·시장 변화의 흐름 전달(주간 트렌드 브리핑 포함) | 최근 새로 생긴 일·흐름인가? |
| 심층 분석 | 한 주제를 파고든 해석·시사점 | 해석이 절반 이상인가? |
| 활용법·튜토리얼 | 따라 할 수 있는 절차·실습 | 따라 하면 재현되는가? |
| 도구·프롬프트 | 특정 도구·프롬프트가 주인공인 소개·사용기 | 도구가 주인공인가? |

- 판정 = 질문 하나에 "예"인 성격 선택
- 둘 이상 걸치면 = 글의 무게중심(전달 vs 해석 vs 절차 vs 도구) 기준 1개

## 주제 6값 (단일 선택 — AI 주제 축, 2026-07-23 교체 · 2026-07-30 크리에이티브·미디어 추가)

- 에이전트 — 에이전트·자동 작업 수행 관련
- 모델·플랫폼 — 모델 출시·업데이트·플랫폼 소식
- 워크플로·자동화 — 하네스·파이프라인·업무 자동화 방법
- 거버넌스·리스크 — 정책·보안·윤리·리스크
- 시장·생태계 — 시장 동향·채용·생태계 변화
- 크리에이티브·미디어 — 영상·이미지·음성 등 생성 미디어 도구·활용

## 이미지·고정 필드

- **이미지** = 카드 썸네일(정사각 76×76, radius 10). 이미지(로고 = `/img/logos/`)가 있을 때만 렌더
- **로고 라이브러리** = `app/public/img/logos/` — 글이 특정 도구·조직 중심이면 해당 로고 지정(예: `이미지: /img/logos/claude.svg`). 보유 18종: claude · anthropic · openai · codex(png) · gemini · meta · mistral · perplexity · huggingface · github · copilot · notion · ollama · langchain · n8n · zapier · colab · kaggle. 없는 로고 = 오너에게 요청(리서치 후 보강)
- **이미지 생략 시** = 썸네일 없음(카드 색면이 성격을 말함 — fallback 아이콘 타일 폐지 2026-07-24) — 특정 조직 소속이 아닌 글은 그대로 두면 됨
- **고정** = `true`면 허브 뷰(전체 탭)에서 해당 성격 섹션 최상단 핀. 상시 노출용 기준 글에만 사용

## 본문 자유 원칙

- 본문 구성 = 기고자 재량 (텍스트·이미지·링크·웹 임베드)
- 이미지 = `![대체텍스트](URL)`, 본문 폭(720px) 자동 축소
- 링크 = `[표시 문구](https://...)`
- 강조·목록·인용(콜아웃)·표 등 마크다운 문법 사용 가능
- 카피 톤(제목·요약·칩 등 표면) = 개조식. 본문 = 평서문 단문 허용 — 과장·감탄·권유형 수사 금지는 공통
- **문단은 짧게 끊고 사이에 빈 줄** — 세로로 길어져도 가독성 우선. 3문장 넘는 문단 이어쓰기 금지 (인용 콜아웃 안에서도 `>` 빈 줄로 문단 분리)
- 전문용어 = 첫 등장에서 한 줄로 풀어 쓴다 (예: "에이전틱 AI — 사람 지시 없이 여러 단계 작업을 알아서 처리하는 AI")

## 본문 서식 확장 (2026-07-25 신설)

일반 마크다운에 더해 아래 전용 블록 사용 가능. 렌더 규칙 = `app/src/pages/Markdown.jsx`.

**핵심 요약 박스** — 글 맨 위 3~4줄 요약. 바쁜 독자는 이것만 읽어도 되게:

```
::: 요약
- 첫째 핵심
- 둘째 핵심
:::
```

**수치 카드** — 핵심 숫자를 큰 타이포로. 형식 = `숫자 | 설명 | 출처` (출처 생략 가능하나 표기 권장):

```
::: 수치
88% | AI 상시 사용 조직 | McKinsey
23% | 에이전트 확장 단계 | McKinsey
:::
```

**표** — GFM 표 문법 그대로 (`| 항목 | 값 |`). 비교·정리용.

**소제목 넘버링** — 본문 `## 소제목`은 기사 상세에서 01·02·03 번호와 구분선이 자동으로 붙음(별도 표기 불필요).

**핵심구 강조** — `*이 문구*` = 강조색(버건디) 굵은 글씨. 섹션당 1~2곳만(남발 시 강조 의미 소실). 숫자·고유명사 강조는 `**볼드**`.

**용어 설명(각주)** — 전문용어는 본문에 풀어 쓰지 말고 유니코드 위첨자(¹ ² ³)로 표시 후 **해당 섹션(## 소제목 단위) 끝**에 모은다. 번호는 섹션마다 1부터 다시 시작:

```
본문에서 에이전틱 AI¹는 …

::: 용어
에이전틱 AI | 여러 단계 작업을 알아서 처리하는 AI
:::
```

**리서치 출처 모음** — 글 맨 아래에 인용한 출처를 한번에. 형식 = `이름 | URL | 비고(선택)`:

```
::: 출처
McKinsey — The State of AI | https://... | 88%·23% 수치
:::
```

## 여정·도판 블록 (2026-07-28 신설)

프로젝트·회고형 글에서 여정과 의사결정을 구조로 담는 블록. 본문 텍스트는 720px이지만 **아래 블록은 자동으로 더 넓게 배치**된다(수치·로드맵·결정·분기점 = 최대 1040px / 비교·단독 이미지 = 풀폭) — 별도 표기 불필요.

**타임라인** — `날짜 | 제목 | 한 줄(선택)`. 제목 앞 `⭐` = 대형 분기점 마커(글당 1~2개만). 데스크톱=가로 레일, 모바일=세로:

```
::: 로드맵
07-01 | ⭐교재 단일원천 전환 | 만든 자료 전량 폐기
07-07 | 진행도 재정의 | 완료 일차/25로 교체
:::
```

**결정 카드** — `문제 | 결정 | 근거(선택) | 폐기한 대안(선택)`. 해당 시점 섹션 안에 배치(상단 집약 금지):

```
::: 결정
수기 집계인가 | 웹앱 제작 | 매주 걷는 수고 제거 | 수기 집계 유지
:::
```

**분기점 보록** — 프로젝트의 최대 분기점 1~2개만. 5행 고정 라벨(순서 고정, 누락 행 미표시):

```
::: 분기점
문제 | 무엇이 막혔나
따진 대안 | 무엇과 비교했나
결정 | 무엇을 택했나
버린 것 | 무엇을 포기했나
결과 | 수치로 확인된 결과
:::
```

**이미지 비교** — `경로 | 캡션`. 2항목=전→후 나란히, 4항목=2×2 갤러리. 경로는 사이트 내부(`/`) 한정. **재현·목업 이미지는 캡션에 반드시 명기**(실캡처로 위장 금지):

```
::: 비교
/img/a.png | 개편 전 — 당시 렌더 방식으로 재현
/img/b.png | 개편 후 — 라이브 화면
:::
```

# 기고 가이드 — 세미나

세미나(`/seminars/`) 기고 규칙. 흐름·파일명·카피 톤은 위 인사이트와 동일(폴더만 `app/content/세미나/`, 템플릿 = `app/content/세미나/_template.md`).

## frontmatter 필드

| 필드 | 필수 | 값 |
|------|------|-----|
| `title` | 필수 | 세미나 제목 한 줄 (개조식·AI투 금지) |
| `author` | 필수 | 발제자id (파일명 프리픽스와 동일) |
| `date` | 필수 | `YYYY-MM-DD`. 타임라인의 정렬 축 · 미래 날짜면 "예정" 배지 |
| `회차` | 필수 | 숫자만 |
| `유형` | 필수 | `인지` \| `실습` |
| `주제` | 선택 | 단일값 — 기사 주제 enum 재사용(원천 `app/src/content/schema.js`). 목록 상단 주제 필터 탭의 축 |
| `발원기사` | 선택 | 이 세미나를 촉발한 인사이트 기사 URL |
| `슬라이드` | 선택 | 사후 발표자료 링크 — 목록 썸네일 카드 클릭 시 새 탭으로 열림 |
| `썸네일` | 선택 | 이미지 경로 1~2개 배열 — 목록의 슬라이드 겹침 카드용. 없으면 카드 비표시 |
| `장소` | 선택 | 오프라인 위치 또는 온라인 (예: Zoom) |
| `일정미정` | 선택 | boolean (기본 false). true = 개최일 미확정 — date와 무관하게 "예정" 취급, 날짜 자리에 "일정 미정" 표기 |
| `요점` | 선택 | 문자열 1~4개 배열 — 목록 항목의 요약 불릿. 생략 시 본문 서문 발췌 |

## 목록 표시 방식 (타임라인 아카이브 — 2026-07-25 v3)

- 세미나 목록 = **단일 세로 날짜 타임라인**(역시간순 기본). 예정/과거를 두 영역으로 가르지 않는다 — **NEXT 히어로·지난 세미나 아코디언은 폐지**
- 맨 위 항목(정렬 기준 최신) = 제목 타이포 확대
- 상단 필터 바 = 주제 탭(콘텐츠에 실제 등장하는 주제만) + 정렬 토글(최신순 ↔ 과거순)
- **"예정" 배지 조건** = `date`가 오늘보다 엄격히 이후 **또는** `일정미정: true`. 오늘 날짜 = 예정 아님
- `일정미정: true`면 날짜 레일에 날짜 대신 "일정 미정"이 표기된다(과거 date를 넣어도 동일). 날짜가 확정되면 `date` 수정 + `일정미정` 삭제
- 날짜 도래 시 배지는 자동으로 사라진다 — 별도 조작 없음

## 본문 3블록 (유형=실습 필수)

- `유형: 실습` = 본문에 `## 준비` / `## 진행` / `## 재현 가이드` 헤딩 3개 필수 (Carpentries 이식 — 누락 시 CI 배포 차단)
- `유형: 인지` = 본문 자유 구성
- 3블록 위 서문(첫 단락) = 상세 밴드 리드 + `요점` 생략 시 목록 발췌로 쓰임
