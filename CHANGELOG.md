# 변경 이력

AIM 허브 사이트의 릴리스 기록. 최신이 위에 온다.

읽는 법 — `추가`는 새로 생긴 것, `수정`은 고친 것, `내부`는 화면에 안 보이는 것.
버전 규칙 = 기능 추가는 가운데 자리(0.9.0 → 0.10.0), 수정만이면 끝자리(0.9.0 → 0.9.1).
절차·기준 = `erp-club/docs/specs/2026-08-19-릴리스-체계.md`

---

## v0.12.0 (2026-08-19)

### 추가
- 프로젝트에 **AIM 허브 사이트 전용 상세 페이지**. 상단 = 프로젝트 헤더(목적·만든 사람·기간·역할·스택·상태·라이브 링크),
  가운데 = 공개 6면 실물 캡처와 각 면을 그렇게 만든 판단·AI 활용, 하단 = **세로 지그재그 로드맵**(분기 18개,
  큰 전환점 6개는 큰 점으로 강조). 구 마크다운 상세(가로 스크롤 여정)를 대체한다.
- 특강 「질문에서 위임으로」 발표자료를 정규 차시와 같은 **덱 서식(30장, 모션 포함)**으로 재구축.
  세미나 페이지에서 슬라이드와 PDF 받기가 함께 열린다.
- 워크스페이스 캘린더에 **분류 색**. AIM(세미나·모집·과제·세션)은 유일한 채움 색으로 가장 도드라지고,
  공고는 보드와 같은 색을 쓴다(채용=노랑·공모전=네이비·자격증=초록·교내활동=틸). 범례·모바일 도트도 동일.

### 수정
- 프로젝트 목록 카드와 홈 카드의 허브 사이트 커버를 **현재 화면 실물**로 교체(옛 캡처 삭제).
- 공개 6면 캡처 전량 재촬영(과거 버전 화면 잔재 제거).
- 교내활동 색이 회색에서 틸로. 회색은 "알 수 없는 분류" 폴백 전용이 됐다.

### 내부
- 분류 색을 한 곳(스타일 단일원천)으로 모아 공고 보드·캘린더·범례가 같은 정의를 읽는다.
- 이벤트 종류에 'AIM'을 추가하는 마이그레이션 **0020** 준비(오너 SQL 실행 대기 — 미적용이어도 화면은 정상,
  단 적용 전 운영 탭에서 종류 AIM 저장은 오류).
- 커버 캡처 규격(16:9)을 공개 면 캡처 명령에 추가 — 재생성이 명령 1줄.

## v0.11.1 (2026-08-19)

### 수정
- 공고 보드 안내가 길고 사실과 달랐다. 코멘트를 폐지했는데도 "왜 유효한가 한 줄이 붙는다"고
  안내하고 있었다. 조작법 3줄로 줄임 — 줄 누르면 원문 이동 / ★ = 내 관심 / 등록한 분류만 캘린더에.
- 운영 탭 공고 등록에서 코멘트를 필수로 막던 문제. 선택 항목이 됐다.

## v0.11.0 (2026-08-19)

### 추가
- 공고 검색창. 제목·주최·출처로 찾는다. 낱말을 여러 개 넣으면 모두 포함하는 것만 남는다.
- 공고에 출처 표시. 링커리어·잡코리아·사람인·광운대 중 어디서 온 링크인지 배지로 보인다.
- 공고 종류에 **교내활동** 신설. 광운대 비교과·특강·튜터·장학이 여기 모인다.
- 자격증에 어학 분류. TOEIC·OPIc이 목록에 들어왔다.
- 공고 11건 → 28건. 공모전·채용·자격증을 다시 훑어 채웠다.

### 수정
- 공고 목록이 카드 2열에서 **행 1열**로 바뀜. 한 줄이 한 공고이고, 줄을 누르면 원문 사이트로 바로 간다.
  카드 높이가 제각각이라 시선이 지그재그로 튀던 문제와, 한 화면에 4~5건뿐이던 문제를 함께 고쳤다.
- 공고 정렬 = 내가 ★ 표시한 것이 맨 위, 그 아래는 접수 임박순.
- 접수 전 공고는 마감일 대신 **시작일**을 보여준다. 아직 열지도 않은 창을 놓친 것처럼 읽히던 문제.
- 공지가 접힌 목록으로 바뀜. 제목과 날짜만 보이고 누르면 펼쳐진다. 최신 1건만 열린 채 시작한다.
- 워크스페이스 홈 상단 정리. 이름을 크게 두고 일정은 칩으로. 첫 로그인 안내 줄은 뺐다.
- 로그인 화면에서 계정 발급 안내를 빼고 "AIM 스터디원 전용입니다." 한 줄만 남겼다.
- 모집 페이지 톤을 낮췄다. "이런 분을 찾습니다" → "이 정도면 충분합니다",
  "전공 무관" → "비전공자 환영", "코딩 경험 무관" → "코딩 몰라도 됨".

### 내부
- 공고 수집 기준을 문서 한 곳에 고정(`ops/공고-수집-하네스.md`). 자동 루틴 2종이 이 문서를 읽는다.
- 공고 코멘트가 선택 항목이 됨. 링크를 모으는 것이 목적이라 한 줄 이유를 강제하지 않는다.

## v0.10.0 (2026-08-19)

### 추가
- 프로젝트 기록에 면별 설계 근거 — 화면마다 왜 존재하는지와 지금 모습을 만든 결정.
- 공개 면 캡처를 명령 한 줄로 다시 만드는 도구. 홈과 ADsP 상세 캡처를 새로 넣음.

### 수정
- 모집 요강에서 값과 부연이 한 줄로 붙어 나오던 문제. 이제 두 줄로 나뉨.
- 푸터를 두 줄로 줄임. 첫 줄 AIM, 둘째 줄 GitHub·이메일·버전.

### 내부
- 프로젝트 기록 수치 현행화(기사 23·테스트 399), 화면 캡처 6장 갱신.

## v0.9.0 (2026-08-19)

베타 기준선. 이 시점의 라이브 상태를 0.9.0으로 고정하고, 여기서부터 변경을 모아서 내보낸다.
이전 변경은 소급해 적지 않는다(기록은 git 이력에 있다).

### 추가
- 푸터에 현재 버전 표기.
